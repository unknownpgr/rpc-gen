{
  "name": "rpc-gen",
  "version": "1.0.7",
  "description": "Solution for generating RPC code from TypeScript code",
  "main": "lib/index.js",
  "license": "MIT",
  "bin": "cli.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/unknownpgr/rpc"
  },
  "homepage": "https://github.com/unknownpgr/rpc",
  "scripts": {
    "build": "tsc",
    "preversion": "yarn build; git add -A lib; git diff-index --quiet HEAD || git commit -m \"build\"",
    "patch": "git add .; git diff-index --quiet HEAD || git commit -m \"patch\"; npm version patch; npm publish"
  },
  "dependencies": {
    "@types/node": "^18.11.18",
    "ts-morph": "^17.0.1",
    "typescript": "^4.9.4"
  },
  "devDependencies": {},
  "keywords": [
    "typescript",
    "rpc",
    "autogenerate"
  ]
}
